extends layout

block content
  h1 PDF Blend
  p A simple web tool to merge PDFs — with or without removing pages visually.

  h2 Choose an action:

  ul
    li
      a(href='/merge') ➕ Merge PDFs (Standard)
    li
      a(href='/merge-with-preview') ✂️ Merge PDFs with Visual Page Removal
    li
      a(href='/remove') 🧼 Remove Pages from a Single PDF

  br
  p You can upload multiple PDF files or even a ZIP archive, and optionally remove pages before combining.
