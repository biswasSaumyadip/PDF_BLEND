extends layout

block content
  h1 PDF Blend – Visual Page Removal
  p Upload your PDF files to visually remove pages before merging.

  form#uploadForm(action='/pdf/merge' method='POST' enctype='multipart/form-data')
    input(type='file' id='fileInput' name='files' multiple accept='application/pdf')
    br
    label(for='filename') Enter name for merged PDF:
    input(type='text' id='filename' name='filename' placeholder='MergedFileName.pdf')
    br
    button(type='submit') Merge PDFs

  #loader(style='display:none;') Loading...

  #previewContainer

  script(src='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.min.js')
  script(type='module' src='/javascripts/preview.mjs') // handles visual preview